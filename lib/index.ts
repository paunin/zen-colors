export { FluidColors } from './FluidColors';
export { presets, ember, amber, glacier, subtle, aurora, sunset, ocean, neon } from './presets';
export type {
  FluidColorsProps,
  BlobConfig,
  BlobShape,
  AnimationConfig,
  AnimationType,
  EasingType,
  BlendMode,
  FluidColorsPreset,
} from './types';
export type { PresetName } from './presets';
